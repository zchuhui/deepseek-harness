{
  "name": "dsh-desktop-shell",
  "version": "0.1.0-rc.5",
  "private": true,
  "description": "DeepSeek Harness desktop shell (Tauri 2): hosts the local dsh web runtime and the native bridge. Kept outside the root pnpm workspace by design (see README).",
  "scripts": {
    "tauri": "tauri",
    "icon": "tauri icon icon-source.png",
    "stage-runtime": "node scripts/stage-runtime.mjs",
    "gen-installer-art": "node scripts/gen-installer-art.mjs",
    "build:release": "node scripts/build-and-sign.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0",
    "sharp": "^0.33.5"
  }
}
