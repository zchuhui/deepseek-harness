/**
 * `@deepseek-ai/dsh-workspace-notes-bundle` is a profile-patch carrier.
 * The profile composer resolves its `cordis.patch.yml` through the manifest;
 * this module deliberately exports no runtime API.
 * @module @deepseek-ai/dsh-workspace-notes-bundle
 */

export {}
